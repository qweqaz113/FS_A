# Skill Evolution

分析主实验错误，并将失败经验整理为可复用技能材料。

```powershell
uv run python run_error_analysis.py --help
uv run python run_package_materializer.py --help
```

默认分析 `../main_agent/runs/paper_initial/`，结果写入 `runs/default/`。
